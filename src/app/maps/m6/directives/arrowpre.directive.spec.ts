import { ArrowpreDirective } from './arrowpre.directive';

describe('ArrowpreDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrowpreDirective();
    expect(directive).toBeTruthy();
  });
});
