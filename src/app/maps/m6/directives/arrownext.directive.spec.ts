import { ArrownextDirective } from './arrownext.directive';

describe('ArrownextDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrownextDirective();
    expect(directive).toBeTruthy();
  });
});
