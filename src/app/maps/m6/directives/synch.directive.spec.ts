import { SynchDirective } from './synch.directive';

describe('SynchDirective', () => {
  it('should create an instance', () => {
    const directive = new SynchDirective();
    expect(directive).toBeTruthy();
  });
});
